# Cryptography program that can create codes and encode/decode messages

A cryptography program that I made to create somewhat complex codes with different patterns and offsets. The program can create number and letter codes. It saves the codes in a json file and you can share the codes with other people through the file. There is a fully functional GUI to create codes and encode and decode messages.
